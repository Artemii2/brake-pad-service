# Куда Vite проксирует /api в режиме npm run dev (бэкенд rip2026)
VITE_PROXY_TARGET=http://localhost:8080

# Прямой base URL axios (обычно пусто — тогда запросы идут на /api через proxy)
# VITE_API_BASE_URL=
